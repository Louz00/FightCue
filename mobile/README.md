# Mobile

This folder will contain the Flutter app for Android and iOS.

## Planned next step

Generate the Flutter project shell once the Flutter SDK is available on this machine.

## Current status

- Shared code structure has been prepared manually
- English, Dutch, and Spanish are planned from day one
- Temporary working application ID base is `com.lou.fightcue`
- Native `android/` and `ios/` runner folders are still pending because `flutter create` cannot run without Flutter installed

## Planned responsibilities

- local timezone-first event UI
- following and alert preferences
- calendar handoff
- push notification registration
- native billing flows
