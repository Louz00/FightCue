This folder preserves the pre-v2 FightCue mobile feature layout.

Baseline commit:

- `f4582c0` - `Document UI v2 direction and fix Android permission flow`

Purpose:

- keep a restorable copy of the current mobile feature structure before the v2 navigation and layout redesign begins
- allow direct comparison between the old layout and the upcoming v2 implementation
